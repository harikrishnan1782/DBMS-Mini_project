<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Secure Payment</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container d-flex align-items-center justify-content-center" style="min-height: 100vh;">
        <div class="glass-panel p-0 overflow-hidden" style="width: 100%; max-width: 480px;">
            <div class="p-4 text-center" style="background: rgba(255,255,255,0.02); border-bottom: 1px solid var(--ios-glass-border);">
                <small class="text-white-50 fw-bold">TOTAL DUE</small>
                <h1 class="my-1 text-white">$<%= bookingData.total_amount %></h1>
                <small class="text-white-50">Flight <%= bookingData.flight.flight_number %> • <%= bookingData.seats %> Seat(s)</small>
            </div>

            <div class="p-4">
                <div class="mb-4 p-4 rounded-4 position-relative overflow-hidden" 
                     style="background: linear-gradient(135deg, #0f2027, #203a43, #2c5364); height: 200px; box-shadow: 0 10px 30px rgba(0,0,0,0.3);">
                    
                    <div class="d-flex justify-content-between align-items-start mb-4">
                        <span class="text-white-50 small ls-1">DEBIT</span>
                        <span class="text-white fw-bold fst-italic">VISA</span>
                    </div>
                    
                    <h4 class="text-white font-monospace my-4" id="cardDisplay" style="letter-spacing: 2px;">•••• •••• •••• ••••</h4>
                    
                    <div class="d-flex justify-content-between mt-auto">
                        <div>
                            <small class="text-white-50 d-block" style="font-size: 0.6rem;">CARD HOLDER</small>
                            <span class="text-white small"><%= bookingData.passenger_name.toUpperCase() %></span>
                        </div>
                        <div>
                            <small class="text-white-50 d-block" style="font-size: 0.6rem;">EXPIRES</small>
                            <span class="text-white small">MM/YY</span>
                        </div>
                    </div>
                </div>

                <form action="/confirm_booking" method="POST" id="paymentForm">
                    <input type="hidden" name="payment_method" value="Credit Card">
                    
                    <div class="mb-3">
                        <label class="form-label">Card Number</label>
                        <input type="text" name="card_number" id="cardInput" class="form-control" placeholder="0000 0000 0000 0000" maxlength="19" required>
                    </div>

                    <div class="row g-3 mb-4">
                        <div class="col-6">
                            <label class="form-label">Expiry</label>
                            <input type="text" class="form-control" placeholder="MM/YY">
                        </div>
                        <div class="col-6">
                            <label class="form-label">CVV</label>
                            <input type="password" class="form-control" placeholder="•••">
                        </div>
                    </div>

                    <div class="alert alert-info bg-transparent border-0 text-info small py-0 mb-4">
                        <i class="me-1">ⓘ</i> <b>Demo:</b> Press <b>Ctrl+Q</b> to auto-fill.
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-success btn-lg rounded-pill">Pay & Confirm</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script>
        const input = document.getElementById('cardInput');
        const display = document.getElementById('cardDisplay');
        input.addEventListener('input', (e) => { display.innerText = e.target.value || '•••• •••• •••• ••••'; });
        document.addEventListener('keydown', (e) => {
            if (e.ctrlKey && e.key === 'q') {
                input.value = '4242 4242 4242 4242';
                display.innerText = '4242 4242 4242 4242';
            }
        });
    </script>
</body>
</html>