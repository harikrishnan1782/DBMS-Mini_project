<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>E-Ticket</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .ticket-cut { width: 30px; height: 30px; background: var(--ios-bg); border-radius: 50%; position: absolute; bottom: 85px; z-index: 10; }
    </style>
</head>
<body>
    <div class="container d-flex flex-column align-items-center justify-content-center" style="min-height: 100vh;">
        <div class="no-print mb-4 text-center">
            <h2 class="mb-1">Boarding Pass</h2>
            <p class="text-white-50">Ready for departure.</p>
        </div>

        <div class="glass-panel p-0 position-relative overflow-hidden" style="width: 100%; max-width: 380px; border-radius: 24px;">
            <div class="p-4 text-center bg-white bg-opacity-10">
                <small class="text-white-50 fw-bold ls-1">HORIZON AIRWAYS</small>
            </div>

            <div class="p-4 pt-5 pb-5">
                <div class="d-flex justify-content-between align-items-center mb-1">
                    <h1 class="display-4 fw-bold mb-0"><%= booking.source_code %></h1>
                    <div class="text-ios-blue fs-4">✈</div>
                    <h1 class="display-4 fw-bold mb-0"><%= booking.dest_code %></h1>
                </div>
                <div class="d-flex justify-content-between text-white-50 small mb-5">
                    <span><%= booking.source_city %></span>
                    <span><%= booking.dest_city %></span>
                </div>

                <div class="row g-4">
                    <div class="col-6">
                        <small class="text-white-50 d-block text-uppercase" style="font-size: 0.65rem;">Passenger</small>
                        <span class="fw-bold"><%= booking.passenger_name %></span>
                    </div>
                    <div class="col-6">
                        <small class="text-white-50 d-block text-uppercase" style="font-size: 0.65rem;">Flight</small>
                        <span class="fw-bold"><%= booking.flight_number %></span>
                    </div>
                    <div class="col-6">
                        <small class="text-white-50 d-block text-uppercase" style="font-size: 0.65rem;">Date</small>
                        <span class="fw-bold"><%= new Date(booking.departure_time).toLocaleDateString() %></span>
                    </div>
                    <div class="col-6">
                        <small class="text-white-50 d-block text-uppercase" style="font-size: 0.65rem;">Time</small>
                        <span class="fw-bold"><%= new Date(booking.departure_time).toLocaleTimeString([], {hour:'2-digit', minute:'2-digit'}) %></span>
                    </div>
                </div>
            </div>

            <div class="ticket-cut" style="left: -15px;"></div>
            <div class="ticket-cut" style="right: -15px;"></div>
            <div style="border-top: 2px dashed rgba(255,255,255,0.1); margin: 0 20px;"></div>

            <div class="p-4 text-center">
                <div style="height: 40px; background: repeating-linear-gradient(90deg, white 0, white 2px, transparent 2px, transparent 5px); width: 80%; margin: 0 auto; opacity: 0.8;"></div>
                <small class="text-white-50 d-block mt-2 font-monospace"><%= booking.booking_id %>-HZN</small>
            </div>
        </div>

        <div class="mt-4 d-flex gap-3 no-print">
            <button onclick="window.print()" class="btn btn-primary rounded-pill px-4">Print</button>
            <a href="/dashboard?uid=<%= booking.user_id %>" class="btn btn-outline-light rounded-pill px-4">Done</a>
        </div>
    </div>
</body>
</html>