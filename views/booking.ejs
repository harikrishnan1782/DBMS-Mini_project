<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Confirm Booking</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">HORIZON</a>
        </div>
    </nav>

    <div class="container" style="padding-top: 100px;">
        <div class="row justify-content-center">
            <div class="col-md-7">
                
                <div class="glass-panel mb-4 p-4 text-center">
                    <small class="text-white-50 fw-bold ls-1">SELECTED FLIGHT</small>
                    <h1 class="my-3 text-white"><%= flight.source_code %> ➝ <%= flight.dest_code %></h1>
                    <div class="d-flex justify-content-center gap-4 text-white-50">
                        <span>Flight <%= flight.flight_number %></span>
                        <span>•</span>
                        <span><%= new Date(flight.departure_time).toLocaleDateString() %></span>
                        <span>•</span>
                        <span style="color: var(--ios-green);">$<%= flight.base_fare %>/seat</span>
                    </div>
                </div>

                <div class="glass-panel p-5">
                    <h4 class="mb-4">Passenger Details</h4>
                    
                    <form action="/payment" method="POST">
                        <input type="hidden" name="flight_id" value="<%= flight.flight_id %>">
                        <input type="hidden" name="user_id" value="<%= user.user_id %>">
                        
                        <div class="mb-3">
                            <label class="form-label">Full Name</label>
                            <input type="text" class="form-control" name="Your Name" required>
                        </div>

                        <div class="mb-4">
                            <label class="form-label">Email Address</label>
                            <input type="text" class="form-control" name="Email" required>
                        </div>

                        <div class="mb-5">
                            <label class="form-label">Passengers</label>
                            <% if (flight.available_seats > 0) { %>
                                <select name="seats" class="form-select" required>
                                    <% const max = Math.min(9, flight.available_seats); %>
                                    <% for(let i=1; i<=max; i++) { %>
                                        <option value="<%= i %>"><%= i %> Person<%= i>1?'s':'' %></option>
                                    <% } %>
                                </select>
                                <div class="form-text text-success mt-2">
                                    ✓ <%= flight.available_seats %> seats available
                                </div>
                            <% } else { %>
                                <div class="alert alert-danger rounded-4">Flight Sold Out</div>
                            <% } %>
                        </div>

                        <div class="d-grid gap-3">
                            <% if (flight.available_seats > 0) { %>
                                <button type="submit" class="btn btn-primary btn-lg">Continue to Payment</button>
                            <% } %>
                            <a href="/" class="btn btn-outline-light rounded-pill">Cancel</a>
                        </div>
                    </form>
                </div>

            </div>
        </div>
    </div>
</body>
</html>