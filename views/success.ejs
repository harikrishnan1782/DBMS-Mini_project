<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Success</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container d-flex align-items-center justify-content-center" style="min-height: 100vh;">
        <div class="glass-panel text-center p-5" style="max-width: 500px;">
            <div style="font-size: 4rem; color: var(--ios-green); margin-bottom: 20px;">✓</div>
            <h2 class="mb-2">Booking Confirmed</h2>
            <p class="text-white-50 mb-4">Your seat is secured. Prepare for takeoff.</p>
            
            <div class="bg-white bg-opacity-10 rounded-4 p-4 text-start mb-4">
                <div class="d-flex justify-content-between mb-2">
                    <span class="text-white-50">Booking Ref</span>
                    <span class="font-monospace fw-bold text-white">#<%= booking.booking_id %></span>
                </div>
                <div class="d-flex justify-content-between mb-2">
                    <span class="text-white-50">Flight</span>
                    <span class="text-white"><%= booking.flight_number %></span>
                </div>
                <div class="d-flex justify-content-between">
                    <span class="text-white-50">Amount</span>
                    <span class="text-white fw-bold">$<%= booking.total_amount %></span>
                </div>
            </div>

            <div class="d-flex gap-3 justify-content-center">
                <a href="/" class="btn btn-outline-light rounded-pill px-4">Home</a>
                <a href="/ticket/<%= booking.booking_id %>" class="btn btn-primary rounded-pill px-4">View Ticket</a>
            </div>
        </div>
    </div>
</body>
</html> 