<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Horizon - Premium Travel</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <span style="color: var(--ios-blue);">●</span> HORIZON
            </a>
            <div class="d-flex gap-3">
                <a href="/login" class="btn btn-sm btn-space rounded-pill px-4">Log In</a>
                <a href="/register" class="btn btn-sm btn-primary rounded-pill px-4">Sign Up</a>
            </div>
        </div>
    </nav>

    <div class="container" style="padding-top: 120px; padding-bottom: 50px;">
        <div class="row w-100 justify-content-center">
            <div class="col-lg-10 text-center">
                
                <h1 class="display-title mb-3">Travel Effortlessly.</h1>
                <p class="lead text-white-50 mb-5">Experience the future of aviation with Horizon.</p>

                <div class="glass-panel p-4 mx-auto mb-5" style="max-width: 900px;">
                    <form action="/search" method="POST">
                        <div class="row g-3 align-items-end">
                            <div class="col-md-4 text-start">
                                <label class="form-label">Origin</label>
                                <select name="source" class="form-select form-select-lg" required>
                                    <option value="" selected disabled>Where from?</option>
                                    <% airports.forEach(airport => { %>
                                        <option value="<%= airport.airport_id %>"><%= airport.city %> (<%= airport.airport_code %>)</option>
                                    <% }); %>
                                </select>
                            </div>
                            <div class="col-md-4 text-start">
                                <label class="form-label">Destination</label>
                                <select name="destination" class="form-select form-select-lg" required>
                                    <option value="" selected disabled>Where to?</option>
                                    <% airports.forEach(airport => { %>
                                        <option value="<%= airport.airport_id %>"><%= airport.city %> (<%= airport.airport_code %>)</option>
                                    <% }); %>
                                </select>
                            </div>
                            <div class="col-md-2 text-start">
                                <label class="form-label">Date</label>
                                <input type="date" name="date" class="form-control form-control-lg" required min="<%= new Date().toISOString().split('T')[0] %>">
                            </div>
                            <div class="col-md-2">
                                <button type="submit" class="btn btn-primary w-100 h-100">Search</button>
                            </div>
                        </div>
                    </form>
                </div>

                <% if (upcomingFlights && upcomingFlights.length > 0) { %>
                    <div class="text-start mt-5">
                        <h4 class="text-white mb-4 ms-2">Upcoming Departures</h4>
                        <div class="row g-4">
                            <% upcomingFlights.forEach(flight => { %>
                                <div class="col-md-6 col-lg-4">
                                    <div class="glass-panel p-4 h-100 d-flex flex-column align-items-start">
                                        <div class="d-flex justify-content-between w-100 mb-3">
                                            <span class="badge bg-white bg-opacity-10 text-white fw-normal"><%= flight.flight_number %></span>
                                            <span class="text-success fw-bold">$<%= flight.base_fare %></span>
                                        </div>
                                        
                                        <div class="d-flex align-items-center gap-3 mb-2 w-100">
                                            <h3 class="mb-0 text-white"><%= flight.source_code %></h3>
                                            <div class="flex-grow-1 border-top border-secondary border-opacity-50"></div>
                                            <h3 class="mb-0 text-white"><%= flight.dest_code %></h3>
                                        </div>
                                        
                                        <div class="d-flex justify-content-between w-100 text-white-50 small mb-4">
                                            <span><%= flight.source_city %></span>
                                            <span><%= flight.dest_city %></span>
                                        </div>

                                        <div class="mt-auto w-100">
                                            <div class="d-flex justify-content-between align-items-center mb-3 text-white-50 small">
                                                <span>📅 <%= new Date(flight.departure_time).toLocaleDateString() %></span>
                                                <span>⏰ <%= new Date(flight.departure_time).toLocaleTimeString([], {hour:'2-digit', minute:'2-digit'}) %></span>
                                            </div>
                                            <a href="/book/<%= flight.flight_id %>" class="btn btn-space w-100 rounded-pill btn-sm">Book Now</a>
                                        </div>
                                    </div>
                                </div>
                            <% }); %>
                        </div>
                    </div>
                <% } %>

            </div>
        </div>
    </div>
</body>
</html>